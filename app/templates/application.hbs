<section id="todoapp">
    <header id="header">
        <h1>todos</h1>
    </header>
     <div class="pull-right">
    {{#if session.currentUser}}
      <span {{action 'logout'}} style="cursor: pointer;">Logout {{session.currentUser.mailId}}</span>
    {{else}}
      {{#link-to 'users'}}Login{{/link-to}}
    {{/if}}
    ---
    {{#link-to 'users'}}Users{{/link-to}}
  </div>

    {{outlet}}
</section>

<footer id="info">
    <p>Double-click to edit a todo</p>
</footer>